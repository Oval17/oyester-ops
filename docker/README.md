# Docker

> Part of [Oyester-Ops](../readme.md)

## Goals

- [ ] Images vs containers, Dockerfile layers, caching
- [ ] `docker build`, `docker run`, volumes, env vars, networking
- [ ] Multi-stage builds for Node apps

See `Dockerfile.example`. Build with:

```bash
docker build -f Dockerfile.example -t oyester-ops:dev ../..
```
