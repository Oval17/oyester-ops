# Helm

> Part of [Oyester-Ops](../readme.md)

## Goals

- [ ] Charts, `values.yaml`, templating, releases
- [ ] `helm lint`, `helm template`, `helm upgrade --install`
- [ ] Environment-specific values (`values-prod.yaml`)

Minimal skeleton: `Chart.yaml` + `values.yaml`.
