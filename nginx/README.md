# NGINX

> Part of [Oyester-Ops](../readme.md)

## Goals

- [ ] Reverse proxy to Node (`proxy_pass`), static files
- [ ] `server` / `location` blocks, headers, gzip
- [ ] TLS termination (high level), reload with `nginx -t && nginx -s reload`

See `nginx.conf` for a minimal reverse-proxy example.
